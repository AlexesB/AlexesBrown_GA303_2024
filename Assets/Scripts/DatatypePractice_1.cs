using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DatatypePractice_1 : MonoBehaviour
{
    // Start is called before the first frame update
    //First assignment is getting nicknames to spell out some word/phrase using different variables

    public char nolan = 'r';
    public char west = 'o';
    public char chen = 'k';
    public char bradford = 'i';
    public char lopez = 'e';
    public char grey = 'T';
    public char wesley = 'h';
    
 
    void Start()
    {

         

        Debug.Log(nolan.ToString() + west.ToString() + west.ToString() + chen.ToString() + bradford.ToString() + lopez.ToString() "is a fun show to watch");


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
